# Q6 Answer template

def solution(arr):
    if len(arr) == 1:
        arr[0] = -1
    else:
        arr.sort(reverse = True)
        arr.pop()
    return arr

arr = [4, 3, 2, 1]
answer = solution(arr)
print(answer)